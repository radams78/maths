\section{Order Theory}

\begin{definition}[Convex]
    Let $X$ be a linearly ordered set and $Y \subseteq X$. Then $Y$ is \emph{convex} if and only if, for
    all $a, b \in Y$ and $c \in X$, if $a < c < b$ then $c \in Y$.
\end{definition}